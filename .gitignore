# Plugin-local settings (per-project, auto-created with documented defaults by the pipeline —
# normally `specify` at the backbone start, or `implement`). The glob covers sdd.local.md.
.claude/*.local.md

# Implementation engine worktrees
.worktrees/

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/
